<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ config('app.name', 'manageX') }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-gray-100 font-sans antialiased">

    <div x-data="{ sidebarOpen: false }" class="flex h-screen overflow-hidden">
        
        <aside 
            :class="{'translate-x-0': sidebarOpen, '-translate-x-full': !sidebarOpen}" 
            class="fixed inset-y-0 left-0 z-50 w-64 bg-slate-900 text-white transform transition-transform duration-300 ease-in-out lg:translate-x-0 lg:static lg:inset-0">
            
            <div class="h-16 flex items-center px-6 text-2xl font-bold border-b border-slate-800">
                Empreint<span class="text-blue-500">e</span>
            </div>

            <nav class="flex-1 px-4 py-4 space-y-1 overflow-y-auto text-sm">
                <a href="{{ route('dashboard') }}" class="flex items-center px-4 py-3 rounded-lg bg-blue-600 text-white">
                    <i class="fas fa-th-large w-5 mr-3"></i> Dashboard
                </a>
                <a href="{{ route('presences') }}" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-user-clock w-5 mr-3"></i> Présences
                </a>
                <a href="{{ route('tasks.index') }}" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-tasks w-5 mr-3"></i> Tâches
                </a>
                <a href="{{ route('conges') }}" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-calendar-alt w-5 mr-3"></i> Congés
                </a>
                <a href="#" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-file-invoice-dollar w-5 mr-3"></i> Paie
                </a>
                <a href="#" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-users w-5 mr-3"></i> Utilisateurs
                </a>
                <a href="{{ route('messages') }}" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-comments w-5 mr-3"></i> Messages
                </a>

                <div x-data="{ open: false }">
                    <button @click="open = !open" class="w-full flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition focus:outline-none">
                        <i class="fas fa-folder-open w-5 mr-3"></i> 
                        <span class="flex-1 text-left">Documents</span>
                        <i class="fas fa-chevron-down text-xs transition-transform" :class="{'rotate-180': open}"></i>
                    </button>
                    <div x-show="open" x-transition class="bg-slate-800/50 rounded-lg mt-1 ml-4 space-y-1 py-2">
                        <a href="{{ route('documents.recus') }}" class="block px-8 py-2 text-xs text-gray-400 hover:text-white">Docs reçus</a>
                        <a href="{{ route('documents.envoyes') }}" class="block px-8 py-2 text-xs text-gray-400 hover:text-white">Docs envoyés</a>
                    </div>
                </div>

                <a href="#" class="flex items-center px-4 py-3 text-gray-400 hover:bg-slate-800 hover:text-white transition">
                    <i class="fas fa-poll w-5 mr-3"></i> Sondages
                </a>
            </nav>

            <div class="p-4 border-t border-slate-800">
                <div class="text-[10px] text-gray-500 text-center uppercase tracking-widest">
                    2025 ManageX — Dev by BigS
                </div>
            </div>
        </aside>

        <div class="flex-1 flex flex-col min-w-0 overflow-hidden">
            
            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-4 lg:px-8">
                <button @click="sidebarOpen = true" class="text-gray-500 lg:hidden focus:outline-none">
                    <i class="fas fa-bars text-xl"></i>
                </button>

                <div class="hidden md:flex relative w-64">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-400">
                        <i class="fas fa-search"></i>
                    </span>
                    <input type="text" class="block w-full pl-10 pr-3 py-2 border border-gray-200 rounded-md bg-gray-50 text-sm focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="Rechercher...">
                </div>

                <div class="flex items-center space-x-4">
                    <div class="flex flex-col text-right hidden sm:flex">
                        <span class="text-sm font-bold text-gray-800">dev ultim</span>
                        <span class="text-[10px] text-blue-500 font-semibold uppercase">Administrateur</span>
                    </div>
                    <div class="h-10 w-10 bg-blue-600 rounded-full flex items-center justify-center text-white font-bold shadow-lg">
                        DU
                    </div>
                    <form method="POST" action="{{ route('logout') }}" class="ml-2">
                        @csrf
                        <button type="submit" class="text-gray-400 hover:text-red-500 transition">
                            <i class="fas fa-power-off"></i>
                        </button>
                    </form>
                </div>
            </header>

            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100 p-4 lg:p-8">
                @yield('content')
            </main>
        </div>
    </div>

    @if (app()->environment('local'))
        @vite(['resources/css/app.css', 'resources/js/app.js'])
    @else
        <link rel="stylesheet" href="{{ asset('build/assets/app-BiXEtUy6.css') }}">
        <script src="{{ asset('build/assets/app-BXS-Op9n.js') }}" defer></script>
    @endif
</body>
</html>
